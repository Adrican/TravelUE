<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beet.application.travelue">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.beet.application.travelue.SignupActivity"
            android:label="@string/title_activity_signup"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.beet.application.travelue.SignupActivity2"
            android:label="@string/title_activity_signup2"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.beet.application.travelue.LoginActivity"
            android:label="@string/title_activity_login"
            android:screenOrientation="portrait">

        </activity>
        <activity
            android:name="com.beet.application.travelue.ResetPasswordActivity"
            android:label="@string/title_activity_reset_password"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.beet.application.travelue.SplashScreen"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.beet.application.travelue.CreateRouteMap"
            android:label="@string/title_activity_create_route_map"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.beet.application.travelue.PaginaPrincipalRutas"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait" />
        <activity android:name="com.beet.application.travelue.SearchActivity"
            android:screenOrientation="portrait" />
        <activity android:name="com.beet.application.travelue.Profile"
            android:screenOrientation="portrait" />
        <activity android:name="com.beet.application.travelue.ContactoSeleccionado"
            android:screenOrientation="portrait" />
        <activity android:name="com.beet.application.travelue.ChatRoom"
            android:screenOrientation="portrait" />
    </application>

</manifest>